import {
  StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
  header: {
    alignItems: 'center',
    flexDirection: 'row',
  },
  headerText: {
    
  },
  headerImage: {
    borderRadius: 30,
    height: 40,
    margin: 10,
    width: 40
  }
})

export default styles;
