import {
  StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
  header: {
    alignItems: 'center',
    flexDirection: 'row',
  },
  headerText: {
    
  },
  headerImage: {
    borderRadius: 30,
    height: 50,
    margin: 10,
    width: 50
  }
})

export default styles;
